@{
    ViewData["Title"] = "Tasks";
    Layout = "_AdminLayout";
}
@model IEnumerable<Student_Performance_Management_System.Models.Tasks>;
<div class="container-fluid">

    <!-- Page Header -->
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h4 class="fw-semibold">Tasks</h4>

        <a asp-action="AddTask" class="btn btn-primary">
            <i class="bi bi-plus-circle"></i> Add Task
        </a>
    </div>

    <!-- task Table -->
    <div class="card shadow-sm">
        <div class="card-body p-0">
            <table class="table table-hover mb-0 align-middle">
                <thead class="table-light">

                    <tr>
                        <th>Id</th>
                        <th>Title</th>
                        @* <th>Description</th> *@
                        <th>Assigned To</th>
                        <th>Course</th>
                        <th>Group</th>
                        <th>Subject</th>
                        @* <th>Status</th *@
                        <th>Valid Till</th>
                        <th class="text-center">Actions</th>
                    </tr>
                </thead>

                <tbody>
                   
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>@item.TasksId</td>
                            <td>@item.Title</td>
                           @* <td>@item.Description</td> *@
                            <td>@item.Staff.Name</td>
                            <td>@item.Course.CourseName</td>
                            <td>@item.CourseGroup.GroupName</td>
                            <td>@item.Subject.SubjectName</td>
                            @* <td>@item.Status</td> *@
                            <td>@item.ValidTo.Date</td>
                            <td class="text-center">
                                <a asp-action="Edit" asp-route-id="1"
                                   class="btn btn-sm btn-outline-warning me-1">
                                    <i class="bi bi-pencil"></i>
                                </a>

                                <a asp-action="Delete" asp-route-id="1"
                                   class="btn btn-sm btn-outline-danger"
                                   onclick="return confirm('Are you sure you want to delete this student?');">
                                    <i class="bi bi-trash"></i>
                                </a>
                            </td>
                        </tr>
                    }

                </tbody>

            </table>
        </div>
    </div>

</div>

